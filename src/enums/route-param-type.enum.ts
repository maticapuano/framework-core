export enum RouteParamType {
    REQUEST = "req",
    RESPONSE = "res",
    NEXT = "next",
    BODY = "body",
    PARAM = "params",
    QUERY = "query",
    HEADERS = "headers",
    FILE = "file",
    FILES = "files",
    REMOTE_IP = "ip",
}
