export enum Constants {
    Controller = "controller",
    Params = "params",
    Middleware = "middleware",
    Path = "path",
    Method = "method",
    HttpStatusCode = "httpStatusCode",
    Header = "header",
    Request = "request",
}
